
```bash
# Habilitar/desabilitar ADOM
config system global
    set adom-status {enable | disable}
end

# Copiar policy entre ADOMs
execute fmpolicy copy-adom-object

# Debug de upgrade de ADOM
diagnose debug enable
diagnose debug service cdb 255
```

## 9) Importar objetos entre ADOMs


```bash
execute fmpolicy copy-adom-object
```