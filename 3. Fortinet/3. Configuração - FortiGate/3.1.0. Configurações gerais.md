### Reset total da configuração

Se for preciso restaurar o equipamento para o padrão de fábrica:

```bash
execute factoryreset
```

> **Atenção:** esse comando apaga a configuração atual do sistema.

### Configuração de DNS

```bash
config system dns
    set primary 192.168.1.3
    set secondary 212.97.32.2
    set autosvr disable
    set dns-cache-limit 5000
    set cache-notfound-responses disable
end
```

### Tempo de expiração da sessão

```bash
config system session-ttl
    set default 43200
    config port
        edit 23
            set timeout 43200
        next
    end
end
```

### Parâmetros globais de administração

```bash
config system global
    set admin-port 80
    set admin-sport 443
    set admin-ssh-port 22
    set admin-telnet-port 23
    set hostname "FortiGate 60B"
    set ntpserver "time.ien.it"
    set ntpsync enable
    set tcp-halfclose-timer 43200
end
```
#### VDOM

```bash
# Habilitar VDOMs
config system global
    set vdom-admin enable
end
```

### 16.2 Console

```bash
config system console
    set mode line
    set baudrate 9600
    set output more
    set login [enable | disable]
end
```

### 16.3 Porta de Gerência

```bash
config system global
    set admin-sport <porta>
end
```

### 16.4 Log em Disco

```bash
config log disk setting
    set status enable
end

# Habilitar log de pacotes inválidos (anti-spoof)
config log setting
    set log-invalid-packet enable
end
```

### 16.5 NAT Central

```bash
config system settings
    set central-nat {enable|disable}
end
```

### 16.6 Backup e Restore

```bash
# Desabilitar reboot automático após restaurar backup
config system global
    set reboot-upon-config-restore disable
end
```

### 7.1 TTL global

```bash
config system session-ttl
    set default 3600
end
```

### 7.2 Timers específicos

bash

Copiar![](data:image/svg+xml;utf8,%0A%3Csvg%20xmlns%3D%22http%3A%2F%2Fwww.w3.org%2F2000%2Fsvg%22%20width%3D%2216%22%20height%3D%2216%22%20viewBox%3D%220%200%2016%2016%22%20fill%3D%22none%22%3E%0A%20%20%3Cpath%20d%3D%22M10.8%208.63V11.57C10.8%2014.02%209.82%2015%207.37%2015H4.43C1.98%2015%201%2014.02%201%2011.57V8.63C1%206.18%201.98%205.2%204.43%205.2H7.37C9.82%205.2%2010.8%206.18%2010.8%208.63Z%22%20stroke%3D%22%23717C92%22%20stroke-width%3D%221.05%22%20stroke-linecap%3D%22round%22%20stroke-linejoin%3D%22round%22%2F%3E%0A%20%20%3Cpath%20d%3D%22M15%204.42999V7.36999C15%209.81999%2014.02%2010.8%2011.57%2010.8H10.8V8.62999C10.8%206.17999%209.81995%205.19999%207.36995%205.19999H5.19995V4.42999C5.19995%201.97999%206.17995%200.999992%208.62995%200.999992H11.57C14.02%200.999992%2015%201.97999%2015%204.42999Z%22%20stroke%3D%22%23717C92%22%20stroke-width%3D%221.05%22%20stroke-linecap%3D%22round%22%20stroke-linejoin%3D%22round%22%2F%3E%0A%3C%2Fsvg%3E%0A)

```bash
config system global
    set tcp-halfclose-timer 120
    set tcp-halfopen-timer 10
    set tcp-timewait-timer 1
    set udp-idle-timer 180
end
```

### 7.3 Ideia prática

- Ajustes de TTL e timers ajudam a controlar consumo de sessão e comportamento de tráfego em ambientes mais restritos

### 10.1 Log local em disco

```bash
config log disk setting
    set maximum-log-age <integer>
    set status enable
end
```

### 10.2 Verificação do uso do disco

```bash
diagnose sys logdisk usage
```

### 10.3 Log remoto

- **Syslog**
- **FortiCloud**
- **FortiSIEM**
- **FortiAnalyzer**
- **FortiManager**

### 10.4 Porta comum para envio de logs

- **TCP 514**

### 10.5 Teste de log


```bash
diagnose log test
```

### 1.2 Security Rating agendado


```bash
config system global
    set security-rating-run-on-schedule [enable | disable]
end
```

- Habilita ou desabilita a execução automática do **Security Posture / Security Rating**.

### 1.3 Rodar o rating manualmente


```bash
diagnose report-runner trigger
```