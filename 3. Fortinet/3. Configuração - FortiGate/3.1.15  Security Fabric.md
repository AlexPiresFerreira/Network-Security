###  Configuração


```bash
# Desativar sincronização de configuração upstream → downstream
config system csf
    set configuration-sync local
end

# Desativar transferência de objetos do fabric
config system csf
    set fabric-object-unification local
```