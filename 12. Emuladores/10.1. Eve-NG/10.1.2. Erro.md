# Erro

``` 
"No IP address on interface pnet0"
Verificar o IP
ip add show pnet0

Reset setup wizard:
rm -f /opt/ovf/.configured
```

- Fix login web 
```
/opt/unetlab/wrappers/unl_wrapper -a restoredb
```

